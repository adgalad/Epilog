record p :-
	character P.

record r :-
	character Y,
	boolean L,
	^r Z,
	p MyP.



procedure main (integer Hola, integer Chao) :-
	character[10} K,
	if (K[1] = 'c') -> character K end,
	integer G,
	for integer I from 1 to 6 -> character K end.
	integer L
	.
