integer X is 5.
integer Y.	

either pedro :-
	pedro I,
	integer I,
	character U,
	integer U,
	integer G, integer H.

record r :-
	character Y,
	^r Z.


procedure main (integer Hola) :-
	integer G is ((5)),
	character C is 'c',
	C is 5,
	for integer I 
		from 1 to 5 -> 
			integer H is 5,
			if H > 6 -> character P is 'c' end
		end,
	integer U.