procedure uno () :-
  integer P,
  integer Y,
  integer Z.

record a :-
  integer X,
  character C.

procedure dos () -> a :-
  integer Q.

procedure main ()
  :- integer A
  , for integer i
  ,  while A > 1 -> write A
                  , A is A - 1
         ; true  -> read A
         ; true  -> write dos()
     end
  .
