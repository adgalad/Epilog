integer WWW is 999.

procedure uno () :-
  integer P is 8,
  integer Y,
  integer Z.

record a :-
  integer X,
  character C.

procedure dos () -> integer :-
  integer Q,
  ^ integer Pointy,
  make Pointy,
  Pointy^ is 5,
  answer 666.

procedure main ()
  :- integer A
  , for A from 1 to 10 -> write A
    end



  ,  while A > 1 -> write A
                  , A is A - 1
         ; true  -> read WWW
         ; true  -> write dos()
     end
  .
