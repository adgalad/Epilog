procedure main () :-

		integer K,
		integer L is 10,
		integer H is 20,
    for K from L to H -> write K
        ; from 70 to 69  ->
        		character W,
            for W from 'b' to 'p' ->
                write W
            end
    end.
