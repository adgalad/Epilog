procedure main() :-
	integer H is 6+5 div 6*7,
	character Y is 'c',
	float[4}[4}[5} F,

	H is 6,
	Y is 'a',
	F[1][2][3] is 0.12,
	if 5 > H -> 
		for integer Hg from 1 to 10  -> 
				while H > 5 -> 
					case H of
						1 -> integer H
					end
				end;
			from 2 to 120 -> character Jg 
		end
	end.
