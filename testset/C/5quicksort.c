/* Quicksort «en sitio».
 *
 * El programa debe operar en un ciclo infinito, preguntando al usuario si
 * desea ordenar números enteros, números en punto flotante, caracteres o
 * terminar. Si el usuario decide continuar, el programa solicitará la
 * cantidad de elementos a ordenar, como un número entero N entre 1 y 100.
 * Una vez obtenido N, el programa solicitará los datos del tipo adecuado,
 * y los almacenará en un arreglo del tipo adecuado, para luego ordenarlos
 * con el método «Quicksort», presentando el arreglo como resultado.
 * Tendrá que escribir tres funciones QuickSort con cuerpo muy similar, pero
 * firma diferente.
 * Quicksort «en sitio» necesita una operación swap para intercambiar los
 * valores en dos posiciones de un arreglo. Resuelva esto usando funciones
 * auxiliares con pasaje por referencia, i.e.

 * void swapi(var int a, var int b) { int t; t = a; a = b; b = t; }

 * No puede escribir esta función usando apuntadores para simular pasaje
 * por referencia.
 */
